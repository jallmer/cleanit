# Pipeline Timings

Per-SRR processing time measurements for each pipeline stage.

## File Naming

`<SRR>_timings.tsv` — TSV with columns for pipeline stage (FastQC, trimming, alignment, counting), trimming mode, start time, end time, and duration.

## Example

See `PRJNA1014965/` for sample files. Full data available on Zenodo.
